{
  "$comment": [
    "What production's alembic_version is KNOWN to record, and when that was last verified.",
    "",
    "This file exists because promote-to-prod.yml runs on a GitHub-hosted runner with no",
    "route to the production database: it cannot ask. So the guard compares the repo's",
    "alembic head against this declared value instead, and the value is only trustworthy",
    "because updating it is a reviewed commit — see docs/runbooks/ALEMBIC_CONVERGENCE.md.",
    "",
    "It is a LEDGER, not a source of truth. The database is the truth; this records the",
    "last time a human read it with `alembic_converge.py --check` from the janua-api pod.",
    "Stale is the expected failure mode, and it fails SAFE: a ledger left behind makes the",
    "guard demand migrations_acknowledged=true on promotes that may not need it, which",
    "costs an operator one checkbox and one look at the database. The opposite — a ledger",
    "edited forward without running the check — defeats the guard entirely, which is why",
    "verified_at and verified_by are required and why the runbook makes reading the",
    "database the step that precedes editing this file."
  ],
  "recorded_revision": "019_email_first_party_engagement",
  "schema_revision": "019_email_first_party_engagement",
  "verified_at": "2026-09-25",
  "verified_by": "The owner applied docs/ops/sql/019_email_first_party_engagement.sql from main (76e5fb01, #648) in the data/postgres pod on 2026-09-25 (~16:43 CDMX): psql -U postgres -d janua -v ON_ERROR_STOP=1, one transaction; output BEGIN, SET, SET, DO, ALTER TABLE x2, CREATE TABLE, CREATE INDEX, GRANT, UPDATE 1, DO, COMMIT. Verification right after, by the Claude Code orchestrator session, read-only (BEGIN READ ONLY ... ROLLBACK) in the same pod: alembic_version=019_email_first_party_engagement; email_events.source character varying NOT NULL DEFAULT 'webhook'; email_events.possible_prefetch boolean NULL; email_tracking_links owner=enclii, indexes ix_email_tracking_links_email_id and pk_email_tracking_links; janua on email_tracking_links SELECT=true INSERT=true UPDATE=true DELETE=false; janua UPDATE on email_events=false (unchanged); email_events 0 rows; email_tracking_links 0 rows. alembic_converge.py --check was NOT run: the api image still does not ship apps/api/scripts (see notes).",
  "notes": [
    "2026-09-04: 012, 013(absent), 014(absent), 015 and 016 were out of step with alembic_version=011 (see git history of this file).",
    "2026-09-06: converged. 013 and 014 needed the postgres role (users/oauth_clients/alembic_version are owned by `enclii`;",
    "the app role `janua` cannot CREATE INDEX), applied from /app/apps/api with DIRECT_DATABASE_URL, then stamped to 016.",
    "Pre-013 backup on the postgres pod: /tmp/janua-pre-013-20260906T192436Z.dump. 0 duplicate emails, so 013 was safe.",
    "Next time the head moves: apply/stamp in the pod, run alembic_converge.py --check, and refresh this file in the same PR",
    "as the migration — a promote that carries an unrecorded migration stops on the drift guard until then.",
    "2026-09-23: 017 applied by hand (additive; payment_mail_dispatches + guard function + 2 triggers; no existing table changed). Dockerfile.api does not copy apps/api/scripts, so alembic_converge.py must be piped into /tmp for --check; the post-promote pod drift check (Base.metadata vs information_schema) is the runtime verification for this revision.",
    "2026-09-24: 018 applied by hand (additive; email_events + 2 indexes; append-only grants to janua; no existing table changed). Unblocks promotes of #638 and later, which the drift guard had refused since 2026-09-24 01:24Z.",
    "2026-09-25: 019 applied by hand (additive: email_events gains source NOT NULL DEFAULT 'webhook', catalog-only on PostgreSQL 11+, and possible_prefetch; email_tracking_links + 1 index owned by enclii; janua SELECT/INSERT/UPDATE on it, no DELETE; no existing column or grant changed). Required by #648 (first-party open/click measurement): no image carrying #648 may run in production without it."
  ]
}
