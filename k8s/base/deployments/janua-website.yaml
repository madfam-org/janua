apiVersion: apps/v1
kind: Deployment
metadata:
  name: janua-website
  namespace: janua
  labels:
    app: janua-website
    app.kubernetes.io/name: janua-website
    app.kubernetes.io/component: website
    app.kubernetes.io/part-of: janua
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: janua-website
  template:
    metadata:
      labels:
        app: janua-website
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: janua-website
          securityContext:
            privileged: false
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
          image: ghcr.io/madfam-org/janua-website:main
          imagePullPolicy: Always
          ports:
            - containerPort: 4104
              protocol: TCP
          env:
            - name: PORT
              value: "4104"
            - name: HOSTNAME
              value: "0.0.0.0"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 4104
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 4104
            initialDelaySeconds: 5
            periodSeconds: 10
