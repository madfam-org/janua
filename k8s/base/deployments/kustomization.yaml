# Legacy deployments-only kustomization — DEPRECATED (PP.3b).
#
# ArgoCD previously watched this path with digests baked in here.
# Canonical paths are now:
#   - production: k8s/overlays/production/
#   - staging:    k8s/overlays/staging/
#
# This file remains for reference during the ArgoCD migration window.
# Image digests are no longer updated here by CI.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - janua-admin.yaml
  - janua-api.yaml
  - janua-dashboard.yaml
  - janua-docs.yaml
  - janua-website.yaml
