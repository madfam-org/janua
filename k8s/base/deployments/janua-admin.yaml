apiVersion: apps/v1
kind: Deployment
metadata:
  name: janua-admin
  namespace: janua
  labels:
    app: janua-admin
    app.kubernetes.io/name: janua-admin
    app.kubernetes.io/component: admin
    app.kubernetes.io/part-of: janua
spec:
  replicas: 1
  selector:
    matchLabels:
      app: janua-admin
  template:
    metadata:
      labels:
        app: janua-admin
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: janua-admin
          securityContext:
            privileged: false
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
          image: ghcr.io/madfam-org/janua-admin:main@sha256:25fe9e153fac7d639b2aa7aed2ef41c91af049ff74df902c12ba3245d374b41c
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4102
              protocol: TCP
          env:
            - name: PORT
              value: "4102"
            - name: HOSTNAME
              value: "0.0.0.0"
            - name: NEXT_PUBLIC_API_URL
              value: "https://api.janua.dev"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 4102
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 4102
            initialDelaySeconds: 5
            periodSeconds: 10
