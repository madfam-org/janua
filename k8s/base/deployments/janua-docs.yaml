apiVersion: apps/v1
kind: Deployment
metadata:
  name: janua-docs
  namespace: janua
  labels:
    app: janua-docs
    app.kubernetes.io/name: janua-docs
    app.kubernetes.io/component: docs
    app.kubernetes.io/part-of: janua
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: janua-docs
  template:
    metadata:
      labels:
        app: janua-docs
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: janua-docs
          securityContext:
            privileged: false
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          image: ghcr.io/madfam-org/janua-docs:main
          imagePullPolicy: Always
          ports:
            - containerPort: 4103
              protocol: TCP
          env:
            - name: PORT
              value: "4103"
            - name: HOSTNAME
              value: "0.0.0.0"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          startupProbe:
            httpGet:
              path: /health
              port: 4103
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 30
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 4103
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 4103
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: next-cache
              mountPath: /app/.next/cache
      volumes:
        - name: tmp
          emptyDir: {}
        - name: next-cache
          emptyDir: {}
